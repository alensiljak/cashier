/*
    This file is not used. All the configuration is done in main.js, by directly
    passing the config options as an object.
    This file left only for this remark.
 */
module.exports = function (ctx) {
    console.log(ctx)

    extras: [
        'fontawesome-v5',
    ]
}