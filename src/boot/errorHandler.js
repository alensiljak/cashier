/**
 * Global error handler
 */
/*
import { errorAsString } from '../lib/errorHelpers'

export default ({ app, router, store }) => {
  app.config.errorHandler = (err, instance, info) => {
    let msg = errorAsString(err)

    let type_error = typeof err
    console.error(`[errorHandler], ${info}, ${type_error}, ${msg}`)

    alert(msg)
  }

  // Custom warning handler (only runs in development)
  app.config.warnHandler = (warn) => {
    console.warn(`[CUSTOM WARNING] ${warn}`)
  }
}
*/
